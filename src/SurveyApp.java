// Application entry point. Creates the Menu and starts the main loop.
public class SurveyApp {
    public static void main(String[] args) {
        Menu menu = new Menu();
        menu.run();
    }
}
